[pytest]
testpaths = tests
markers =
    integration: tests that mock the secondary API boundary
    contract: tests that hit the real secondary API (requires RUN_CONTRACT_TESTS=1)
    slow: tests that are expected to take longer than usual
